namespace Ambev.DeveloperEvaluation.ORM;

/// <summary>
/// Represents the ORM layer of the system.
/// </summary>
/// <remarks>
/// This class serves as a reference for other layers when they need to access 
/// or reference the assembly of the ORK layer. The assembly can be 
/// retrieved using <c>typeof(ORK).Assembly</c>, which allows 
/// other layers to programmatically reference the application layer's assembly.
/// </remarks>
public class ORMLayer { }